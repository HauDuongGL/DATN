import 'package:flutter/material.dart';

class ProtectTrees extends StatelessWidget {
  const ProtectTrees({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Protect Trees'),
    );
  }
}
